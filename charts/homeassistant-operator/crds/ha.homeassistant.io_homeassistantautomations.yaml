# GENERATED by hack/sync-chart-from-config.sh from config/crd/bases — DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: homeassistantautomations.ha.homeassistant.io
spec:
  group: ha.homeassistant.io
  names:
    categories:
    - homeassistant
    kind: HomeAssistantAutomation
    listKind: HomeAssistantAutomationList
    plural: homeassistantautomations
    shortNames:
    - haauto
    - haautomation
    singular: homeassistantautomation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.homeAssistantRef.name
      name: HomeAssistant
      type: string
    - jsonPath: .spec.alias
      name: Alias
      type: string
    - jsonPath: .spec.enabled
      name: Enabled
      type: boolean
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: HomeAssistantAutomation is the Schema for the homeassistantautomations
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HomeAssistantAutomationSpec defines the desired state of
              HomeAssistantAutomation
            properties:
              actions:
                description: |-
                  Actions define the sequence of tasks to execute when triggered
                  At least one action is required
                items:
                  description: |-
                    AutomationAction defines an action to be executed by the automation
                    This is a flexible structure that accepts any valid Home Assistant action configuration
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                minItems: 1
                type: array
              alias:
                description: Alias is a user-friendly name for the automation
                minLength: 1
                type: string
              autoReload:
                default: true
                description: |-
                  AutoReload enables automatic hot-reload when automation changes
                  If false, requires manual reload or pod restart
                type: boolean
              conditions:
                description: |-
                  Conditions define requirements that must be met for the automation to execute
                  All conditions must evaluate to true for the automation to run
                items:
                  description: |-
                    AutomationCondition defines a condition that must be met for the automation to execute
                    This is a flexible structure that accepts any valid Home Assistant condition configuration
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              description:
                description: Description provides details about what the automation
                  does
                type: string
              enabled:
                default: true
                description: |-
                  Enabled controls whether this automation is active
                  Can be used to temporarily disable without deleting the CR
                type: boolean
              homeAssistantRef:
                description: HomeAssistantRef references the HomeAssistant CR that
                  will use this automation
                properties:
                  name:
                    description: Name of the HomeAssistant resource
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              id:
                description: |-
                  ID is a unique identifier for the automation (used by Home Assistant)
                  If not specified, will be auto-generated from the CR name
                  Must contain only lowercase letters, digits, and underscores. Existing
                  resources with an id predating this constraint (uppercase letters or
                  hyphens) keep working until their next update, which will then be
                  rejected until id is renamed to a conforming value.
                pattern: ^[a-z][a-z0-9_]*$
                type: string
              initialState:
                description: |-
                  InitialState defines whether the automation should be enabled at startup
                  If not specified, the last state is restored
                type: boolean
              max:
                default: 10
                description: Max defines the maximum number of concurrent or queued
                  runs (for queued/parallel modes)
                format: int32
                minimum: 1
                type: integer
              maxExceeded:
                default: warning
                description: MaxExceeded defines the log severity level when max is
                  exceeded (silent, info, warning, error)
                enum:
                - silent
                - info
                - warning
                - error
                type: string
              mode:
                default: single
                description: Mode defines how the automation should behave when triggered
                  again while already running
                enum:
                - single
                - restart
                - queued
                - parallel
                type: string
              triggers:
                description: |-
                  Triggers define the events that will trigger this automation
                  At least one trigger is required
                items:
                  description: |-
                    AutomationTrigger defines an event that will trigger the automation
                    This is a flexible structure that accepts any valid Home Assistant trigger configuration
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                minItems: 1
                type: array
            required:
            - actions
            - alias
            - homeAssistantRef
            - triggers
            type: object
          status:
            description: HomeAssistantAutomationStatus defines the observed state
              of HomeAssistantAutomation
            properties:
              automationHash:
                description: |-
                  AutomationHash is the SHA256 hash of the current automation configuration
                  Used to detect changes and determine if reload is needed
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the HomeAssistantAutomation state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastError:
                description: |-
                  LastError contains the error message from the last failed operation
                  Cleared when operation succeeds
                type: string
              lastReloadMethod:
                description: |-
                  LastReloadMethod indicates how the last reload was performed
                  Possible values: "hot-reload", "restart", "none"
                type: string
              lastReloadTime:
                description: LastReloadTime is the timestamp of the last successful
                  reload
                format: date-time
                type: string
              lastTriggeredTime:
                description: LastTriggeredTime is the timestamp when the automation
                  was last triggered (if available from HA)
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed HomeAssistantAutomation
                format: int64
                type: integer
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.homeAssistantRef is immutable after creation
          rule: '!has(oldSelf.spec) || self.spec.homeAssistantRef == oldSelf.spec.homeAssistantRef'
    served: true
    storage: true
    subresources:
      status: {}
