# GENERATED by hack/sync-chart-from-config.sh from config/crd/bases — DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: homeassistantscripts.ha.homeassistant.io
spec:
  group: ha.homeassistant.io
  names:
    categories:
    - homeassistant
    kind: HomeAssistantScript
    listKind: HomeAssistantScriptList
    plural: homeassistantscripts
    shortNames:
    - hascript
    - hascp
    singular: homeassistantscript
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.homeAssistantRef.name
      name: HomeAssistant
      type: string
    - jsonPath: .spec.alias
      name: Alias
      type: string
    - jsonPath: .spec.mode
      name: Mode
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: HomeAssistantScript is the Schema for the homeassistantscripts
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HomeAssistantScriptSpec defines the desired state of HomeAssistantScript
            properties:
              alias:
                description: Alias is a user-friendly name for the script
                minLength: 1
                type: string
              autoReload:
                default: true
                description: |-
                  AutoReload enables automatic hot-reload when script changes
                  If false, requires manual reload or pod restart
                type: boolean
              description:
                description: Description provides details about what the script does
                type: string
              fields:
                additionalProperties:
                  description: |-
                    ScriptField defines an input parameter for the script
                    This is a flexible structure that accepts any valid Home Assistant field configuration
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                description: |-
                  Fields define input parameters for the script
                  These allow the script to accept arguments when called
                type: object
              homeAssistantRef:
                description: HomeAssistantRef references the HomeAssistant CR that
                  will use this script
                properties:
                  name:
                    description: Name of the HomeAssistant resource
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              icon:
                description: |-
                  Icon is a Material Design icon for the script (e.g., "mdi:script", "mdi:play")
                  See https://mdi.bessarabov.com/ for available icons
                type: string
              id:
                description: |-
                  ID is a unique identifier for the script (used by Home Assistant)
                  If not specified, will be auto-generated from the CR name
                  Must contain only lowercase letters, digits, and underscores
                pattern: ^[a-z][a-z0-9_]*$
                type: string
              max:
                default: 10
                description: Max defines the maximum number of concurrent or queued
                  runs (for queued/parallel modes)
                format: int32
                minimum: 1
                type: integer
              maxExceeded:
                default: warning
                description: MaxExceeded defines the log severity level when max is
                  exceeded (silent, info, warning, error)
                enum:
                - silent
                - info
                - warning
                - error
                type: string
              mode:
                default: single
                description: Mode defines how the script should behave when called
                  again while already running
                enum:
                - single
                - restart
                - queued
                - parallel
                type: string
              sequence:
                description: |-
                  Sequence defines the list of actions to execute when the script is called
                  At least one action is required
                items:
                  description: |-
                    ScriptAction defines an action to be executed by the script
                    This is a flexible structure that accepts any valid Home Assistant action configuration
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                minItems: 1
                type: array
            required:
            - alias
            - homeAssistantRef
            - sequence
            type: object
          status:
            description: HomeAssistantScriptStatus defines the observed state of HomeAssistantScript
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the HomeAssistantScript state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastError:
                description: |-
                  LastError contains the error message from the last failed operation
                  Cleared when operation succeeds
                type: string
              lastReloadMethod:
                description: |-
                  LastReloadMethod indicates how the last reload was performed
                  Possible values: "hot-reload" (success), "failed" (all retries exhausted), "none" (skipped/initial)
                type: string
              lastReloadTime:
                description: LastReloadTime is the timestamp of the last successful
                  reload
                format: date-time
                type: string
              lastRunTime:
                description: LastRunTime is the timestamp when the script was last
                  executed (if available from HA)
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed HomeAssistantScript
                format: int64
                type: integer
              scriptHash:
                description: |-
                  ScriptHash is the SHA256 hash of the current script configuration
                  Used to detect changes and determine if reload is needed
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.homeAssistantRef is immutable after creation
          rule: '!has(oldSelf.spec) || self.spec.homeAssistantRef == oldSelf.spec.homeAssistantRef'
    served: true
    storage: true
    subresources:
      status: {}
