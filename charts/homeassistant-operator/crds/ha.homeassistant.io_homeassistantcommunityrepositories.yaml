# GENERATED by hack/sync-chart-from-config.sh from config/crd/bases — DO NOT EDIT.
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: homeassistantcommunityrepositories.ha.homeassistant.io
spec:
  group: ha.homeassistant.io
  names:
    categories:
    - homeassistant
    kind: HomeAssistantCommunityRepository
    listKind: HomeAssistantCommunityRepositoryList
    plural: homeassistantcommunityrepositories
    shortNames:
    - hacr
    singular: homeassistantcommunityrepository
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.homeAssistantRef.name
      name: HomeAssistant
      type: string
    - jsonPath: .spec.category
      name: Category
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.installedVersion
      name: Version
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HomeAssistantCommunityRepository installs a HACS-compatible community extension
          (integration, plugin, theme, python_script, or template) into an existing
          HomeAssistant instance, without requiring HACS or its UI to be present. This is an
          EXPERIMENTAL, alpha-quality resource: it carries no API stability guarantee between
          releases.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HomeAssistantCommunityRepositorySpec defines the desired
              state of HomeAssistantCommunityRepository
            properties:
              category:
                description: |-
                  Category is the HACS repository category. appdaemon and netdaemon are intentionally
                  not accepted here: they require a separate runtime this operator does not deploy.
                enum:
                - integration
                - plugin
                - theme
                - python_script
                - template
                type: string
              homeAssistantRef:
                description: HomeAssistantRef references the HomeAssistant instance
                  to install this repository into
                properties:
                  name:
                    description: Name of the HomeAssistant resource
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              ref:
                description: |-
                  Ref is the tag, branch, or commit SHA to install. Pinned and explicit — this
                  operator never tracks a "latest" release automatically. Restricted to
                  characters valid in a git ref: word characters, dot, dash and slash, so
                  branch names such as release/v1 are accepted. URL-reserved characters that
                  could alter the codeload request path are not.
                minLength: 1
                pattern: ^[\w][\w.\-/]*$
                type: string
              repository:
                description: Repository is the GitHub "owner/repo" shorthand (not
                  a full URL).
                pattern: ^[\w.-]+/[\w.-]+$
                type: string
            required:
            - category
            - homeAssistantRef
            - ref
            - repository
            type: object
          status:
            description: HomeAssistantCommunityRepositoryStatus defines the observed
              state of HomeAssistantCommunityRepository
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the repository state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              installedVersion:
                description: |-
                  InstalledVersion is the last ref that was successfully validated and activated.
                  It does NOT change until a newly requested ref is fully confirmed, so a failed
                  update never reports a broken version as installed.
                type: string
              installingSince:
                description: |-
                  InstallingSince records when the resource most recently entered the
                  Installing phase. Used to bound the activation retry window; cleared when
                  the resource leaves Installing (Installed or Failed).
                format: date-time
                type: string
              lastError:
                description: |-
                  LastError contains a human-readable error message from the last failed operation.
                  Cleared when the operation succeeds.
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed CR
                format: int64
                type: integer
              phase:
                description: Phase is the current lifecycle phase.
                type: string
              resolvedTarget:
                description: |-
                  ResolvedTarget is the install target computed from the source repository's own
                  manifest (the integration domain, or the theme/script/template/plugin file name).
                  Together with the referenced HomeAssistant name and Category it forms the
                  conflict-detection key, so the same target may be installed into two
                  different instances.
                type: string
            type: object
        type: object
        x-kubernetes-validations:
        - message: spec.homeAssistantRef is immutable after creation
          rule: '!has(oldSelf.spec) || self.spec.homeAssistantRef == oldSelf.spec.homeAssistantRef'
        - message: spec.category is immutable after creation
          rule: '!has(oldSelf.spec) || self.spec.category == oldSelf.spec.category'
    served: true
    storage: true
    subresources:
      status: {}
